#include <stdio.h>

/**
 * main - print its program name
 * @argc: arg count
 * @argv: arg vector
 * Return: 0
 */

int main(int __attribute__((unused)) argc,char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
